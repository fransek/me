---
import { cn } from '@/lib/utils'

interface Props {
  className?: string
}

const { className } = Astro.props
---

<div class={cn('bg-white bg-opacity-[3.5%] p-4 rounded-lg', className)}>
  <slot />
</div>
