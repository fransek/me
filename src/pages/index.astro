---
import BaseLayout from '@/components/layouts/BaseLayout.astro'
import { getEntry } from 'astro:content'

const { render } = await getEntry('home', 'home')
const { Content } = await render()
---

<BaseLayout>
  <Content />
</BaseLayout>
